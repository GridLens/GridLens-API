You are configuring a simple mock backend API for my Smart MeterIQ / GridLens app. 

Goal:
Create a very simple Express server with CORS that exposes these endpoints with MOCK data only (no database):

- GET /api/kpi/energy-loss/overview
- GET /api/kpi/energy-loss/feeders
- GET /api/kpi/energy-loss/suspicious-meters
- GET /api/kpi/energy-loss/fieldops
- GET /api/debug/test

I want this to run cleanly on Replit with ONE click on the “Run” button.

Please do the following steps for me:

1) Use index.js as the main entry file
- If index.js already exists, REPLACE its contents completely.
- Put this EXACT code into index.js (you can adjust only the port logic for Replit if needed):

    const express = require("express");
    const cors = require("cors");

    const app = express();
    const PORT = process.env.PORT || 3001;

    app.use(cors());
    app.use(express.json());

    // --- MOCK KPI ENDPOINTS ---

    // Hero metrics / overview
    app.get("/api/kpi/energy-loss/overview", (req, res) => {
      res.json({
        tenant: "HSUD",
        asOfDate: "2025-12-06",
        systemLossPct: 5.8,                    // System Loss % (Electric)
        nonTechnicalLossDollarsMonth: 38200,   // Non-technical loss $/month
        metersInGoodHealthPct: 96.4,           // % meters in good health
        saidiMinutes: 42.3,                    // SAIDI (minutes)
        saifiEvents: 1.8,                      // SAIFI (interruptions/customer)
        lossHotspotFeedersCount: 3,            // # feeders above loss threshold
        revenueRecovered30d: 18750             // Revenue recovered in last 30 days
      });
    });

    // Feeder / zone loss view
    app.get("/api/kpi/energy-loss/feeders", (req, res) => {
      res.json({
        tenant: "HSUD",
        feeders: [
          {
            feederName: "FDR-3",
            zoneName: "North Zone",
            lossPct: 7.5,
            kwhLostMonth: 18000,
            dollarsLostMonth: 9500,
            activeWorkOrders: 2
          },
          {
            feederName: "FDR-5",
            zoneName: "Central Zone",
            lossPct: 5.3,
            kwhLostMonth: 12000,
            dollarsLostMonth: 6400,
            activeWorkOrders: 1
          },
          {
            feederName: "FDR-7",
            zoneName: "South Zone",
            lossPct: 4.1,
            kwhLostMonth: 8200,
            dollarsLostMonth: 4100,
            activeWorkOrders: 0
          }
        ]
      });
    });

    // Suspicious meters table
    app.get("/api/kpi/energy-loss/suspicious-meters", (req, res) => {
      res.json({
        tenant: "HSUD",
        meters: [
          {
            meterId: "E-10021",
            accountName: "Smith Residence",
            location: "120 Oak St",
            pattern: "Suspected bypass",
            estLossDollarsMonth: 220,
            daysInState: 7,
            status: "Open"
          },
          {
            meterId: "E-55192",
            accountName: "Residential",
            location: "309 Pine St",
            pattern: "Zero usage anomaly",
            estLossDollarsMonth: 80,
            daysInState: 11,
            status: "Investigating"
          },
          {
            meterId: "E-88012",
            accountName: "Corner Market",
            location: "510 Main St",
            pattern: "Voltage mismatch + tamper",
            estLossDollarsMonth: 310,
            daysInState: 4,
            status: "In progress"
          }
        ]
      });
    });

    // FieldOps KPI summary
    app.get("/api/kpi/energy-loss/fieldops", (req, res) => {
      res.json({
        tenant: "HSUD",
        summary: {
          openLossWorkOrders: 12,
          avgAgeDays: 5.3,
          truckRollsAvoidedEstimate: 8,
          resolvedLast30d: 19
        }
      });
    });

    // Simple debug endpoint to confirm server is running
    app.get("/api/debug/test", (req, res) => {
      res.json({ ok: true, message: "Debug endpoints working" });
    });

    app.listen(PORT, () => {
      console.log(`Mock backend running on port ${PORT}`);
    });

2) Ensure dependencies are set up
- Open package.json (create it if it does not exist) and make sure:
  - "express" and "cors" are in "dependencies".
  - The project uses CommonJS (NO `"type": "module"` needed).

  Example package.json (you can merge with existing if needed):

  {
    "name": "smart-meteriq-backend-mock",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
      "start": "node index.js"
    },
    "dependencies": {
      "express": "^4.19.0",
      "cors": "^2.8.5"
    }
  }

3) Make sure the Replit “Run” button works
- Configure the Replit run command (in .replit or through the UI) so that clicking “Run” does:
    npm install
    npm start
  or simply runs:
    node index.js
- The important part is: when I press “Run”, the server starts and listens on process.env.PORT.

4) After setup, print for me:
- The full base URL for this Repl, for example:
  https://my-repl-name.username.repl.co
- Confirm that the following endpoints work:
  - GET /api/debug/test
  - GET /api/kpi/energy-loss/overview
  - GET /api/kpi/energy-loss/feeders
  - GET /api/kpi/energy-loss/suspicious-meters
  - GET /api/kpi/energy-loss/fieldops

5) Finally, tell me EXACTLY what value to use in my React app for:
   const API_BASE = "<your-url-here>";
So my frontend can call:
   `${API_BASE}/api/kpi/energy-loss/overview`
etc.

Please now perform all of these steps automatically in this Repl, fix any small issues, and then show me the final index.js, package.json, and the correct API_BASE value to plug into my frontend.
